import CreateTrace from "./_components/create-trace";

export const metadata = {
  title: "Note",
};

export default function NoteCreatePage() {
  return (
    <div>
      <CreateTrace />
    </div>
  );
}
